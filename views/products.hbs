{{> partials/menu}}
<div class="col-lg-9">
    <div class="col-lg-12">
        <h2>Продукти</h2>
    </div>
    <div class="col-lg-12 search_bar">
        <div class="input-group">
            <input type="text" name="product_filter" id="product_filter" class="form-control input-lg" placeholder="Filter products">
            <span class="input-group-btn">
                <button class="btn btn-success btn-lg" id="btn_product_filter">Фільтр</button>
                <a href="/admin/products" class="hidden-xs btn btn-warning btn-lg"><i class="fa fa-times" aria-hidden="true"></i></a>
            </span>
        </div>
        <p class="text-warning top-pad-10">Продукція може бути відфільтрована за назвою продукту або ключовими словами щодо опису продукту</p>
    </div>
    {{#if results}}
        <div class="col-lg-12">
            <ul class="list-group">
                <li class="list-group-item">
                    <span class="pull-right"><strong>Опубліковано</strong></span>
                    <strong>Продукти - <span class="text-danger">Відфільтрований термін: {{searchTerm}} </span></strong>
                </li>
                {{#each results}}
                    <li class="list-group-item">
                        <h4 class="pull-right text-danger" style="padding-left: 10px;"><a class="text-danger" href="/admin/product/delete/{{this._id}}" onclick="return confirm('Дійсно видалити цей продукт?');"> <i class="fa fa-trash-o"></i></a></h4>
                        <h4 class="pull-right"><input id="{{this._id}}" class="published_state" type="checkbox" {{checkedState this.productPublished}}></h4>
                        <h5><a href="/admin/product/edit/{{this._id}}">{{this.productTitle}}</a></h5>
                    </li>
                {{/each}}
            </ul>
        </div>
    {{else}}
        <div class="col-lg-12">
            <ul class="list-group">
                <li class="list-group-item">
                    <span class="pull-right"><strong>Опубліковано</strong></span>
                    <strong>Останні продукти</strong>
                </li>
                {{#each top_results}}
                    <li class="list-group-item">
                        <h4 class="pull-right" style="padding-left: 10px;"><a class="text-danger" href="/admin/product/delete/{{this._id}}" onclick="return confirm('Дійсно видалити цей продукт?');"> <i class="fa fa-trash-o"></i></a></h4>
                        <h4 class="pull-right"><input id="{{this._id}}" class="published_state" type="checkbox" {{checkedState this.productPublished}}></h4>
                        <h5><a href="/admin/product/edit/{{this._id}}">{{this.productTitle}}</a></h5>
                    </li>
                {{/each}}
            </ul>
        </div>
    {{/if}}
</div>